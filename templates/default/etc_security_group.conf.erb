<%= template_header %>

<% @rules.each do |r| -%>
<%- # FIXME: r['grp'] should be an array, not a String -%>
<%= "#{r['srv'] || '*'};#{r['tty'] || '*' };#{r['usr'] || '*' };#{r['time'] || 'Al0000-2400' };#{r['grp'] || ''}" %>
<%- end %>
