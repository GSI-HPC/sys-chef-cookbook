<%= template_header %>

MY_HOSTID="<%= @hostid %>"
PARTICIPATE="<%= @participate?'yes':'no' %>"
USEHTTP="<%= (@use_http == false)?'no':'yes' %>"<%# default to yes %>
<%# in principle we could use rand(7) here but we want something more stable: %>
DAY="<%= @day || @hostid.hex % 7 %>"
ENCRYPT="<%= @encrypt || 'maybe' %>"
