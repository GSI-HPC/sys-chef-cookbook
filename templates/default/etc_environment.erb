# /etc/environment

<%= template_header %>

<% node['sys']['env'].keys.sort_by!(&:downcase).each do |key| %>
<% raise "Whitespace in environment variable name" if key =~ /\s/ %>
<%= key %>="<%= node['sys']['env'][key] %>"
<% end %>
