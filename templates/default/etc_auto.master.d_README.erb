<%= template_header %>

The files in /etc/auto.master.d/ are no longer managed by chef.  Depending on
the configuration of automount, they might not even be considerd anymore.  If
you are missing some mountpoints, check if automount uses /etc/auto.master or
ldap, in which case /etc/auto.master will not exist.